---
import env from '@/env';
import Layout from '@/layout/index.astro';
---

<Layout class="flex flex-1 flex-col items-center justify-center gap-8" title="404">
  <div class="flex flex-col gap-4 text-center">
    <h2 class="text-center text-5xl font-bold">_404_</h2>
    <h1 class="animate-pulse text-2xl font-bold text-primary-main">Ups! Página no encontrada</h1>
  </div>
  <div class="flex flex-col gap-2 text-center">
    <code class="text-sm text-slate-400"
      >{env.APP_NAME}-{env.APP_MODE}-{env.GITHUB_WORKFLOW_SHA?.slice(0, 7)}</code
    >
  </div>
</Layout>
